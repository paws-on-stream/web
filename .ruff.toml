target-version = "py312"
line-length = 88
extend-exclude = [
  "*/migrations/*"
]

[lint]
select = [
  "E", # pycodestyle errors
  "W", # pycodestyle warnings
  "F", # Pyflakes
  "I", # isort
  "UP", # pyupgrade
  "SIM", # flake8-simplify
  "DJ", # flake8-django
]
ignore = []
